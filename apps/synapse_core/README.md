# synapse_core

Headless product core for NSHKR Agent.

This app owns product-safe input normalization, view-model mapping, feature
classification, and later AppKit wrapper modules. It must not own lower
runtime, workflow, memory, provider, connector, policy, or proof behavior.
